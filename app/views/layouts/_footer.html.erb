<div class="footer_content">
    <% if get_all_pages.size > 0 %>
      <% for o_row in get_all_pages %>
        <%= link_to o_row.name, "/#{o_row.page_route}" %> |
      <% end %>
    <% end %>
  <%= t("general.copyright_all_rights_reserved_html") %>
</div>
</div>
</div>